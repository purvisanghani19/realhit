import { createContext } from "react";

const CartContex = createContext();
const LoginContex = createContext();
const PaymentContex = createContext();

export { CartContex, LoginContex, PaymentContex };
